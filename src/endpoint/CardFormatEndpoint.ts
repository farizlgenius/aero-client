const CONTROLLER = 'CardFormat'

export const CardFormatEndpoint = {
    GET_CARDFORMAT : `/api/v1/${CONTROLLER}`,
    CREATE_CARDFORMAT : `/api/v1/${CONTROLLER}`,
    DELETE_CARDFORMAT : `/api/v1/${CONTROLLER}/`
}
